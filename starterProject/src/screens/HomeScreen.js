import React from 'react';
import { Text, StyleSheet } from 'react-native';

const HomeScreen = () => {
  return <Text style={styles.text}>kaisan ba!</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 24,
  },
});

export default HomeScreen;